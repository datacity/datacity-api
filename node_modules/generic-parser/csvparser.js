var csv = require('csvtojson');
var fs = require('fs');
var util = require('util');

var Parser = require('./parser');

var CSVParser = function(file) {
    CSVParser.super_.call(this, file);
}


util.inherits(CSVParser, Parser);

CSVParser.prototype = {
    parseToJson: function() {
        
    }
}

module.exports = CSVParser;
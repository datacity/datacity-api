    var CSVParser = require('./csvparser');

    /**
    * Instancie un parseur en fonction du type de fichier.
    *
    * @param {Object|String} Contenu du fichier dans un objet ou path.
    * @param {String} l'extension du fichier (optionnelle dans le cas ou le contenu du fichier est présent.
    * @return {Object} Retourne l'objet ParserFactory.
    */
    
    var ParserFactory = function(file, ext) {
        this.file = file;
        this.ext = ext;
        console.log("ParseFactory Instanciée!!!!");
    };
  
    ParserFactory.prototype = {
        createParser: function() {
            var types = {
                "csv" : CSVParser,
                "xml" : "",
                "xls" : "",
                "json": ""
            };
            var genericParser = new types[this.ext](this.file);
            genericParser.parseToJson();
        },
        getFile: function() {
        },
        getExtension: function() {
        }
    }
    module.exports = ParserFactory;
var testCsv = require("../index.js");
var fs = require("fs");
testCsv("csv")
.from(fs.createReadStream(__dirname+"/files/input/test.csv"))
.to(__dirname + "/files/output/test1.json");

/**
 * MOCHA TEST : Pour tester ce test unitaire, vous devrez installer mocha et le lancer a partir de cela
 */
describe("Csv parser",function(){
    it ("Should read from an input file, parse it and write in output file",function(){
        testCsv("csv")
        .from(fs.createReadStream(__dirname+"/files/input/test.csv"))
        .to(__dirname + "/files/output/test1.json");
    });
});